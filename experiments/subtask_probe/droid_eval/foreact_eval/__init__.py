"""Faithful ForeAct reconstruction on DROID (inference-only, no training).

See ``README.md`` for scope and the ``FINDINGS.md`` ForeAct release-audit
section for why this module only covers π_v (planner) + π_g (foresight
generator) and not the end-to-end VLA integration the paper reports.
"""
